<!-- DOC_ORG_SCAN: 2026-04-07 | source-scan: pending-manual-by-domain -->

# 1.md

## Ajustes aplicados
- Consolidado o status da compatibilidade de build/dependências do repositório.
- Registrado explicitamente que **as keys de assinatura e placeholders não foram removidas**.
- Documentado os pontos de validação para manter integridade de configuração.

## Keys preservadas (sem remoção)
- `signingConfigs.debug` com `storeFile`, `keyAlias`, `storePassword`, `keyPassword`.
- `signingConfigs.release` com `storeFile`, `keyAlias`, `storePassword`, `keyPassword`.
- `manifestPlaceholders` em `defaultConfig` e `perfRelease`.

## O que foi feito (resumo)
- Arquivo criado para registrar o pedido: fazer os ajustes **sem tirar os keys**.
- Mantida a estratégia de compatibilidade sem alterar/remover chaves existentes.
