cmake_minimum_required(VERSION 3.22.1)
project(rafcoder_legacy NONE)

# Legacy android/ módulo: placeholder canônico para manter externalNativeBuild válido.
# A trilha oficial e suportada de build nativo permanece em :app (raiz do repositório).
